/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: { unoptimized: true },
  basePath: '/agrosaidaa',
  assetPrefix: '/agrosaidaa',
};

export default nextConfig;